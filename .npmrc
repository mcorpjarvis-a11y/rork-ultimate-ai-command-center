# NPM Configuration for RORK AI Command Center

# Use legacy peer dependency resolution to avoid version conflicts
legacy-peer-deps=true

# Save exact versions in package.json
save-exact=true

# Always use package-lock.json
package-lock=true

# Reduce npm noise
loglevel=warn
